# Socket.IO Configuration
# Copy this file to .env.local and update with your actual values

# Socket.IO server URL
# IMPORTANT: Use HTTP/HTTPS protocol (not WS/WSS) for Socket.IO
#
# Path handling:
#   - URL without path: http://server.com -> connects to /socket.io (Socket.IO default)
#   - URL with path: http://server.com/api -> connects to /api
#   - URL with root path: http://server.com/ -> connects to root (/)
#
# Examples:
#   VITE_WS_URL=http://live.mindarae.com:8888          # Uses /socket.io
#   VITE_WS_URL=http://live.mindarae.com:8888/api/ws   # Uses /api/ws
#   VITE_WS_URL=http://live.mindarae.com:8888/         # Uses root path
VITE_WS_URL=http://live.mindarae.com:8888

# Access token (obtain from your authentication service)
VITE_ACCESS_TOKEN=xxxx

# Optional: Override device/client IDs (normally auto-generated)
# VITE_DEVICE_ID=
# VITE_CLIENT_ID=
